[tool.ty.environment]
python-platform = "all"
root = [".", "tests_a7392be"]

[tool.ty.rules]
all = "error"

[tool.uv]
add-bounds = "exact"
exclude-newer = "P5D"
package = false
required-version = ">=0.11.0"
