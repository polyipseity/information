# Feedback log — academic-notes skill

Append new incidents, patterns, and user feedback here with a **date** and a **one-sentence description**. The operational workflow (gather → document → teach validator → verify → PR) lives in the **Continuous improvement** section of `SKILL.md`; use this file to log raw feedback and to preserve provenance.

**When the log gets too long:** fold learnings into the appropriate skill files (`SKILL.md`, `course-template.md`, or `.agents/instructions` files that apply to `special/academia`). Add or clarify rules, examples, and checklist items where they belong, then **remove the incorporated entries from this log** so it stays a short, current list. Git history preserves provenance; no need to keep long historical blocks in this file.

---

- **2026-03-18** — Follow-up correction for honors-probability CDF notes: when a user asks for the “four equivalent conditions” for absolute continuity, use the probability/measure-theoretic quartet if that is what the course is using—density on all Borel sets, absolute continuity of the cumulative distribution function, vanishing probability on Lebesgue-null Borel sets, and the epsilon-delta small-Borel-set criterion—rather than silently switching to a derivative-based formulation. Incorporated into SKILL.md, academic-notes.instructions.md, and `special/academia/HKUST/MATH 2431/cumulative distribution function.md`.
- **2026-03-18** — Follow-up honors-probability CDF refinement: when one of the equivalent conditions is “the cumulative distribution function is absolutely continuous,” do not leave that phrase as a bare label—state the disjoint-interval epsilon-delta definition in both the prose and the corresponding flashcard so readers know exactly what property of the function is being asserted. Incorporated into SKILL.md, academic-notes.instructions.md, and `special/academia/HKUST/MATH 2431/cumulative distribution function.md`.

- **2026-03-18** — In honors-probability CDF notes, when absolute continuity is an important topic, list the four equivalent formulations explicitly in both prose and flashcards: epsilon-delta absolute continuity of $F$, density representation, absolute continuity of the induced measure with respect to Lebesgue measure, and almost-everywhere differentiability with recovery by integrating $F'$. Incorporated into SKILL.md, academic-notes.instructions.md, and `special/academia/HKUST/MATH 2431/cumulative distribution function.md`.

- **2026-03-18** — In honors-probability random-variable notes, use conventional value-space notation such as $(E,\mathcal{E})$ and explain explicitly that a symbol like $B\in\mathcal{E}$ is a measurable subset of the target space whose preimage must lie in $\mathcal{F}$; when discussing countable-image laws, also explain the motivation that the law is then completely determined by the point masses on the actually attained values. Incorporated into SKILL.md, academic-notes.instructions.md, and `special/academia/HKUST/MATH 2431/random variable.md`.

- **2026-03-18** — In honors-probability notes, whenever prose or flashcards warn against a misconception, add a very small explicit counterexample instead of leaving the warning abstract; this is especially helpful for random-variable misconceptions (not every function is measurable, same law does not mean pointwise equality) and CDF misconceptions (same singleton masses, same support, or same moments do not determine the law). Incorporated into SKILL.md, academic-notes.instructions.md, `special/academia/HKUST/MATH 2431/random variable.md`, and `special/academia/HKUST/MATH 2431/cumulative distribution function.md`.

- **2026-03-18** — In honors-probability CDF notes, the abstract pushforward proof should not only define $T_{\ast}$ and preimages, but also explain why the pushforward formula is legitimate as a measure construction: measurability of $T$ makes preimages Borel, and preimages preserve empty sets, whole spaces, and countable disjoint unions, so $A\mapsto \lambda(T^{-1}(A))$ is genuinely a probability measure. The key half-line computation should also be unpacked as a chain of preimage equalities, not collapsed into one jump. Incorporated into SKILL.md, academic-notes.instructions.md, and `special/academia/HKUST/MATH 2431/cumulative distribution function.md`.

- **2026-03-18** — Follow-up honors-probability refinement: proof-oriented flashcards should not only state definitions and results but also hint at the coming proof steps and explain the intuitive meaning of the result just obtained; this is especially useful for the concrete Lebesgue-Stieltjes proof, nested generalized-inverse equivalence clusters, and the abstract pushforward section. When repository memory accumulates stable academic-notes lessons, fold them back into SKILL.md and academic-notes.instructions.md so the guidance becomes persistent rather than memory-only. Incorporated into SKILL.md, academic-notes.instructions.md, `special/academia/HKUST/MATH 2431/cumulative distribution function.md`, and `special/academia/HKUST/MATH 2431/index.md`.

- **2026-03-18** — Honors-probability notes should avoid unresolved references such as lecture numbers or chapter numbers unless the note links directly to a resolvable file/section in the repository; for the Lebesgue-Stieltjes construction, the concrete-proof section should include intuition prose, a flashcard explaining the proof’s purpose, a flashcard explaining both sides of the key event identity in plain language, a bridge from the concrete proof to the abstract pushforward proof, and nested flashcards for multi-step generalized-inverse equivalence arguments. When the abstract proof uses $T_{\ast}$, each occurrence should be expanded enough that readers do not need to remember the notation implicitly from earlier sentences. Incorporated into SKILL.md, academic-notes.instructions.md, and `special/academia/HKUST/MATH 2431/cumulative distribution function.md`.

- **2026-03-18** — When users request refinements to honors-probability proof flashcards, the concrete-existence cards should explicitly state their purpose, the key event-identity card should explain both the left-hand set and the right-hand set in plain language, implication cards should use an auxiliary variable like $s=F^{-1}(u)$ plus a readable target set such as $\{z:F(z)\ge u\}=[s,\infty)$, and the “why the construction gives the target CDF” card should restate the key event identity rather than only alluding to it. Also, continuous-learning review should always be performed when a user requests changes, even if no reusable documentation update is ultimately needed. Incorporated into SKILL.md, academic-notes.instructions.md, and `special/academia/HKUST/MATH 2431/cumulative distribution function.md`.

- **2026-03-18** — Honors probability CDF notes should not assume the inverse-transform heuristic is self-explanatory: explicitly say what the target random variable $X$ is in expressions like $U=F(X)$, present the generalized-inverse equivalence as a multi-step argument rather than one compressed paragraph, and define the pushforward operator symbol $T_{\ast}$ itself (not just $T_{\ast}\lambda$) before using it in prose or flashcards. Incorporated into SKILL.md, academic-notes.instructions.md, and `special/academia/HKUST/MATH 2431/cumulative distribution function.md`.

- **2026-03-18** — Honors probability CDF notes should make the generalized-inverse argument fully explicit: define $A_u=\{y:F(y)<u\}$ and $B_u=\{x:F(x)\ge u\}$, show carefully that $B_u=[\sup A_u,\infty)$ (with right-continuity used only at the boundary), derive rigorously that $F^{-1}(u)\le x$ iff $u\le F(x)$, and in the abstract pushforward proof explicitly define $\lambda_{(0,1)}$, preimages, and pushforward notation in both prose and flashcards rather than assuming the reader already knows them. Incorporated into SKILL.md, academic-notes.instructions.md, and `special/academia/HKUST/MATH 2431/cumulative distribution function.md`.

- **2026-03-18** — Follow-up CDF refinement: when densities exist, equality induced by equal cumulative distribution functions must be stated as equality **almost everywhere** rather than everywhere; CDF notes should explicitly warn that equal CDFs do not imply pointwise-equal PDFs, include both generalized-inverse formulas with a brief equivalence explanation in the pseudoinverse flashcards, and often read better when the Lebesgue-Stieltjes proof is split into a concrete existence/uniqueness subsection followed by a separate abstract pushforward subsection. Incorporated into SKILL.md, academic-notes.instructions.md, `special/academia/HKUST/MATH 2431/cumulative distribution function.md`, and `special/academia/HKUST/MATH 2431/index.md`.

- **2026-03-18** — Honors probability notes on cumulative distribution functions should now go beyond the standard property proofs: explicitly explain equality of distributions as equality of measures / equality of cumulative distribution functions, warn against false heuristics such as matching only atoms, only support, or only moments, elaborate the Cantor-distribution construction stage by stage (including why there are no atoms yet the law is singular), and motivate the pseudoinverse construction via the inverse-transform idea together with an abstract pushforward-on-generators proof view. Incorporated into SKILL.md, academic-notes.instructions.md, `special/academia/HKUST/MATH 2431/cumulative distribution function.md`, `special/academia/HKUST/MATH 2431/random variable.md`, and `special/academia/HKUST/MATH 2431/index.md`.

- **2026-03-18** — In honors probability notes, Dynkin's pi-lambda theorem should include general “how to use it” intuition (small generating pi-system + property-defined Dynkin system + promotion step), and uniqueness-from-agreement proofs should explicitly explain why the agreement class is a Dynkin system and illustrate the method with a standard generator such as half-lines or half-open intervals. Incorporated into SKILL.md, academic-notes.instructions.md, and `special/academia/HKUST/MATH 2431/sigma-algebra.md`.

- **2026-03-18** — In honors probability notes, cumulative-distribution-function sections should prove the standard properties (range, monotonicity, right-continuity, limits at infinity, jump formula) rather than merely list them, and the flashcards should include the proof ideas; likewise, pi-systems and Dynkin systems should be explicitly compared with sigma-algebras, with finite-intersection emphasis for pi-systems, a disjoint-additivity motivation for Dynkin systems, and a more readable disjointification explanation. Incorporated into SKILL.md, academic-notes.instructions.md, `special/academia/HKUST/MATH 2431/cumulative distribution function.md`, and `special/academia/HKUST/MATH 2431/sigma-algebra.md`.
- **2026-03-18** — Added a note that disjointification flashcards are easiest when written as a nested cluster (setup → disjointness → same-union → why it works) and that the final card should explicitly call out the pi-system (finite intersection/difference) vs Dynkin-system (countable disjoint union) roles. Incorporated into SKILL.md and academic-notes.instructions.md.
- **2026-03-18** — In honors probability proofs, do not blindly preserve slide notation when a clearer symbol choice would better separate logical roles (for example generator vs arbitrary generated set in the bootstrap proof of Dynkin's pi-lambda theorem); integrate the intuition directly into the proof steps and reflect the cleaner notation in the flashcards as well. Incorporated into SKILL.md, academic-notes.instructions.md, and `special/academia/HKUST/MATH 2431/sigma-algebra.md`.

- **2026-03-18** — When a broad probability topic note accumulates substantial cumulative-distribution-function material, split that content into its own singularly named topic note (for example `cumulative distribution function.md`) and update the original note, course index children list, and all affected session § links together; in general, topic-specific notes should prefer singular canonical filenames/titles unless the concept is inherently plural. Incorporated into SKILL.md (§ Filenames, titles, and links), academic-notes.instructions.md (§ Topic notes), and `special/academia/HKUST/MATH 2431/continuous distribution.md` / `cumulative distribution function.md` / `index.md`.

- **2026-03-17** — Honors probability notes on generated $\sigma$-algebras should explicitly show that the containing-family in $\sigma(\mathcal{E})$ is nonempty (via $\mathcal{P}(\Omega)$), include the arbitrary-intersection theorem with a short proof sketch, add intuition when the deep non-measurable-set proof is omitted in lecture (interval-length nonextendability on $[0,1)$), and preserve course-provided generator examples in both prose and flashcards. Incorporated into SKILL.md, academic-notes.instructions.md, and `special/academia/HKUST/MATH 2431/sigma-algebra.md`.

- **2026-03-17** — Nested flashcards in academic notes should indent by exactly two spaces per level, and the full-parent-path requirement for nested QA prompts applies to topic notes as well as index/session pages; validator support was added accordingly. Incorporated into SKILL.md (§ Flashcards and markup), academic-notes.instructions.md (§ Quick guidance for agents), rules.py, and tests.

- **2026-03-17** — For approximation or derivation flashcard clusters in academic notes, descriptive step-name prompts on the left should still be accompanied by explicit mathematical terms on the right (for example the exact factorization, key limit expressions, or target PMF) so the strategy is clearer. Incorporated into SKILL.md (§ Flashcards and markup), academic-notes.instructions.md (§ Quick guidance for agents), and `special/academia/HKUST/MATH 2431/discrete distribution.md`.

- **2026-03-17** — In topic-specific notes, flashcard prompts should not repeat the filename/topic prefix because the flashcard viewer already shows the file name; use local labels at top level (for example `definition`, `probability space`) and keep only in-file parent paths for nested clusters (for example `Poisson approximation / statement`). Incorporated into SKILL.md (§ Flashcards and markup), academic-notes.instructions.md (§ Topic notes), validator rule `topic_note_redundant_filename_prefix`, and MATH 2431 topic notes.

- **2026-03-17** — The same topic-note redundancy rule should also catch repetition of the note title itself, not just the filename stem: top-level prompts like `Probability Measure` or `Probability Measure / definition` are redundant because the title is already visible. Incorporated into SKILL.md (§ Flashcards and markup), academic-notes.instructions.md (§ Topic notes), validator rule `topic_note_redundant_filename_prefix`, and its tests.

- **2026-03-17** — When a source heading combines a broad construction with one especially important canonical instance, it is often clearer to split it into a parent section plus a dedicated subsection (for example, `generated sigma-algebras` with subsection `Borel sigma-algebra`) instead of keeping one overloaded heading. Incorporated into SKILL.md (§ Topic-specific notes), academic-notes.instructions.md (§ Topic notes), and `special/academia/HKUST/MATH 2431/sigma-algebra.md`.

- **2026-03-17** — For approximation or derivation flashcard clusters in academic notes, prefer descriptive step-name prompts on the left (for example `Poisson approximation / factor limits`) rather than dumping the full target formula there; if nested proof or intuition steps are useful, express them inline on the right with `<br/>` and labels like `(1)`, `(1.1)`, `(1.2)`, `(2)`. Incorporated into SKILL.md (§ Flashcards and markup) and academic-notes.instructions.md (§ Quick guidance for agents).

- **2026-03-17** — LaTeX on the left-hand side of academic-note flashcards is acceptable when that is the natural prompt; agents should not paraphrase away equations or symbolic expressions merely to avoid left-side math. Incorporated into SKILL.md (§ Flashcards and markup) and academic-notes.instructions.md (§ Quick guidance for agents).

- **2026-03-17** — When editing topic-specific notes, treat prose, flashcards, and course-index links as one synchronized unit: if content changes, revise the corresponding flashcards and update every affected `index.md` section link or anchor in the same task. Incorporated into SKILL.md (§ Key principles, § Authoring workflow, § Course index and outline updates) and academic-notes.instructions.md.

- **2026-03-17** — Topic-note section titles should separate distinct named distributions or concepts when each receives its own explanation (for example, split discrete uniform and Bernoulli into separate sections), and headings should usually use the canonical law name alone when extra model interpretation can live in the prose (for example, “binomial distribution” instead of “binomial distribution and Bernoulli-trial model”). Incorporated into SKILL.md (§ Topic-specific notes) and academic-notes.instructions.md (§ Topic notes).

- **2026-03-17** — Flashcards in academic notes should be written as standalone artifacts: if a card depends on a local hypothesis or setup (for example $f\colon\Omega\to[0,\infty)$ in countable-sum notes, $P[B]>0$ in conditional probability, or “without replacement” in sampling models), restate that condition in the card instead of relying on adjacent cards or prose. Incorporated into SKILL.md (§ Key principles, § Flashcards and markup) and academic-notes.instructions.md.

- **2026-03-08** — Accounting course convention: topic note `journal entries.md` with one entry type per section; each example in a single blockquote (scenario, markdown table Dr/Cr right-aligned, optional explanation). Validator: `no_soft_wrap_paragraph` exempts only **table rows** (after stripping multi-level blockquote prefix `>`, `>>`, `> >`); blockquote prose remains subject to soft-wrap. Incorporated into SKILL.md (§ Journal entries note), academic-notes.instructions.md, rules.py (docstring + `after_quote` logic + test).

- **2026-03-08** — When adding new lecture content: prefer section links if content duplicates existing; enhance existing notes (prose + flashcards) and section-link if content extends existing material; create new notes only when there is no or little overlap. Documented in SKILL.md (§ Adding new lecture content) and academic-notes.instructions.md.
- **2026-03-08** — Journal entry examples: minimum—mask all debit and credit amounts with clozes; also mask scenario with one or more clozes but leave enough hint words; prefer slightly smaller clozes. Use `&nbsp;` instead of comma for three-digit thousands separator for readability. Incorporated into SKILL.md (§ Journal entries note) and academic-notes.instructions.md (§ Topic notes / accounting).
- **2026-03-08** — Topic notes (except journal entries): almost never use cloze ({@{ }@}); use two-sided (::@::) or extremely rarely one-sided (:@:) flashcards only. Incorporated into SKILL.md (§ Flashcards and markup, § Topic-specific notes, Pre-commit checklist), academic-notes.instructions.md (§ Topic notes), flashcard-creation SKILL.md.
- **2026-03-08** — Journal entry scenario clozes: cover almost the entire scenario but leave a few hint words at the beginning or end; for longer scenarios use two or three clozes. Updated SKILL.md, instructions, flashcard-creation skill; applied to ACCT 3020 journal entries.md.
- **2026-03-08** — Accounting courses: do not use LaTeX; use plain text, numbers, and symbols (×, −, ÷). Updated SKILL.md (Key principles, Journal entries note), academic-notes.instructions.md; removed LaTeX from ACCT 3020 current liability.md and journal entries.md.
- **2026-03-10** — ELEC 1100 lab-prep PDFs (Prepare for Lab 1–4): treat tutorial/lab preparation slide decks as normal lecture ingestion; centralise technical content (equipment usage, resistor colour code, DSO reading, diode/LED safety, Zener regulation, transistor switches, H-bridge, L293/74HC14/LM7805 wiring) in topic notes and use session entries mainly for § links plus a few lab-workflow/safety flashcards instead of duplicating prose or worked examples in the index.
- **2026-03-10** — Infinite resistor ladder examples: for “infinite” series/parallel resistor ladders introduced in tutorials, describe the self-similarity method once in the `resistor networks` topic note (model the tail as $R_{\text{eq}}$ and solve an equation like $R_{\text{eq}} = 2R + (1.5R \parallel R_{\text{eq}})$) with generic and course-specific cards; in the index, reference this via the existing resistor-networks § links rather than adding extra session-level ladder derivations.
- **2026-03-10** — Zener clamp graph-reading: when lectures or labs use $V_Z$ vs $V_{\text{in}}$ plots to illustrate clamp behaviour, explain the method in the voltage-regulator topic note (curve follows divider line then flattens at breakdown; read $V_Z$ from the plateau) and add a calculation-style card that repeats givens in the left prompt; in the index, optionally add a short ELEC 1100 / voltage regulator flashcard that points students from the week 5 tutorial to that method.
- **2026-03-10** — Lab-equipment and safety workflows: for course-specific lab procedures (function generator reset + FUNCTION output, reading DSO scales, LED/diode orientation stripe = cathode, LM7805 “gets hot” and regulation check, keeping LM7805/L293/74HC14 circuits on the same breadboard across labs, multimeter mA-terminal safety), capture them as prose and QA cards in the relevant topic notes (`lab equipment.md`, `diode.md`, `voltage regulator.md`, `H-bridge.md`) and surface them from the index via concise lab-prep flashcards instead of embedding long instructions directly into session outlines.
- **2026-03-11** — ELEC 1100 labs and assignment notes: lab assignments live under `labs/lab N/index.md`. These files are assignment-style notes with Canvas-style metadata and attachments/submission sections; they are no longer forced to carry a visible `# index` / `## children` shell because the validator gained file-level suppression via `ignore-file[rule1, rule2]`. Assignment notes do not require per-section flashcard blocks; copy the Canvas description verbatim (using `<br/>` for line breaks where needed) and keep a single source of truth for attachment filenames and submission templates by mirroring the directory structure (handouts in `labs/lab N/attachments/`, submission template in `labs/lab N/`). Lab 2 and Lab 3 manuals (Lab2_2026s.pdf, Lab3_2026s.pdf) reinforced the same pattern: concepts and reusable methods (e.g. infinite ladders, KVL/KCL verification workflows, Zener clamp behaviour, LM7805 line regulation and safety) belong in topic notes and are linked from the index; detailed TA-check steps and numeric tables stay in the PDF and summary sheets and do not need to be duplicated in Markdown.
- **2026-03-11** — Validator suppressions: added file-level suppression support (`ignore-file[rule1, rule2]: rationale`) alongside `ignore-line` and `ignore-next-line`. The redundancy check now treats an `ignore-file` directive as redundant only when the suppressed rule never fires anywhere in the file; this is used for assignment-style index pages (e.g. `labs/lab N/index.md`) that deliberately omit the standard `# index`/`## children` shell while still keeping the rules active for normal course indexes.
- **2026-03-11** — ELEC 1100 Lab 1–3 manuals (`Lab1_2026s.pdf`, `Lab2_2026s.pdf`, `Lab3_2026s.pdf`) confirmed that only cross-lab safety and usage patterns (e.g. DC supply current limiting and GND reference, DSO probe ground connections, LM7805 pin order and line-regulation checks) should be promoted into topic notes and flashcards; fine-grained TA-check steps, question numbering, and detailed tables remain in the PDFs/summary sheets rather than being mirrored in Markdown.
- **2026-03-11** — ELEC 1100 lab session linking: for each timed lab session in the course index (e.g. “week 4 lab 1”, “week 5 lab 1”, “week 6 lab 1”), the outline should both (a) list § links into the relevant topic notes (electronic component, lab equipment, diode, voltage regulator, H-bridge, etc.) and (b) include a bullet linking to the corresponding assignment note under `labs/lab N/index.md` so that the Canvas-style lab page is reachable from the timeline alongside the conceptual material.

- **2026-03-13** — Course index aliases: new course index pages should include the same alias set as exemplars (e.g. ELEC 1100): course code with and without space, each with " index" variant; full course name and " index"; HKUST-prefixed course code and " index", all alphabetically sorted.
- **2026-03-13** — Do not put instructor or TA names or email addresses in course notes; refer to syllabus or LMS for contact; office hours (day/time/format) are fine. Folded into SKILL.md (Pre-commit checklist), academic-notes.instructions.md (Course index and logistics), course-template.md (comment).
- **2026-03-13** — In course index logistics, use separate bullet points for instructor (refer to syllabus/LMS) and office hours (day, time, format).
- **2026-03-13** — Session entry structure (lecture, tutorial, lab): list each topic note as a parent link with indented § lines underneath; for index-only content (e.g. session flashcards), use the course code as parent and nest under it. Incorporated into SKILL.md, academic-notes.instructions.md, course-template.md.
- **2026-03-13** — Flashcard glosses with numbered sub‑points: for right-hand text that “looks like bullet points” (for example `(1)… (2)… (3)…`), keep the gloss on a single line and insert ` <br/> ` between items instead of turning them into a markdown list. Incorporated into SKILL.md (§ Flashcards and markup) and academic-notes.instructions.md (Short author checklist, item 4).
- **2026-03-13** — Do not put LaTeX inside italics or bold; it may not render correctly. Keep math outside emphasis delimiters (e.g. _phrase_ on $math$ not _phrase $math$_).
- **2026-03-13** — Topic-note hierarchy and examples: it is fine to introduce `###` (and deeper) subsections inside topic notes (e.g. finite vs infinite vs continuous sample spaces), but every subsection still needs its own `---` and flashcard block; avoid standalone “Examples” sections and instead fold short worked examples (like the two-roll die calculation) into the parent section’s prose and cards. Incorporated into SKILL.md (§ Topic-specific notes, § Course index and outline updates) and academic-notes.instructions.md (Topic notes).

- **2026-03-13** — Honors math courses (e.g. MATH 2431): when notes give “elementary properties” or similar propositions, include short proof sketches or derivations in the topic notes (one paragraph per major step rather than a single over-dense block) and add corresponding flashcards that summarize both the statement and the key proof idea (e.g. constructing disjoint $B_n$ from $A_n$ for subadditivity or reusing $(\Omega,\mathcal{F})$ when proving $P[\cdot\mid B]$ is a measure). Incorporated into SKILL.md (Key principles: honors courses) and academic-notes.instructions.md (Topic notes honors bullet).
- **2026-03-13** — Course-topic flashcard prefixes: in topic-specific notes within a course folder, omit the course code prefix in flashcard prompts (course context is implied), but keep explicit course-code prefixes for index/session-level cards or other mixed-context pages. Incorporated into SKILL.md and academic-notes.instructions.md.
- **2026-03-13** — Honors math section naming: in probability-topic notes (e.g. conditional probability, independence), prefer descriptive section titles that state both the concept and its role ("definition and multiplication rule", "law of total probability and Bayes' theorem", "independence via cards and coins") and keep session § links in the course index synchronized with those names and anchors.
- **2026-03-13** — Law of total probability derivations: when using the law of total probability and Bayes' theorem in honors probability notes, explicitly define what it means for $(B_j)$ to be a partition of $\Omega$ and include short derivation arguments (e.g. expand $A$ as $\bigcup_j (A \cap B_j)$ and apply $\sigma$-additivity and the multiplication rule) both in the prose and via a small set of focused flashcards (definition of partition, law statement with derivation idea, Bayes formula, and the countable case). Incorporated into SKILL.md (honors guidance) and academic-notes.instructions.md (topic-notes bullet).
- **2026-03-13** — Independence heuristics and closure: in independence topic notes, make explicit that for $P[A],P[B]>0$ the heuristic “conditioning on one does not change the probability of the other” is equivalent to the factorisation $P[A\cap B]=P[A]P[B]$ in both directions, and use the standard coin-toss example to spell out the pairwise-equals-but-not-jointly-independent calculations ($P[A]=P[B]=P[C]=1/2$, all pairs factorise, triple does not). Also, when presenting stability under complements, prefer a short multi-step flashcard chain (statement, split-$A_1$ step, role-swap step, both-complements step) instead of a single dense card. Clarified in SKILL.md and academic-notes.instructions.md as part of honors-course guidance.
- **2026-03-13** — Public-holiday sessions: when adding public-holiday or no-class sessions to course indexes (e.g. “week 3 lecture 1” for Lunar New Year’s Day), always include `datetime:` and a `status: public holiday: <name>` or `status: no class` line and omit `topic:` and § links; this keeps the timeline chronological without suggesting content coverage. Existing SKILL.md and academic-notes.instructions.md logistics rules already encode this pattern; no further changes needed.

---

(Append new entries right above the horizontal rule with date and short description.)
