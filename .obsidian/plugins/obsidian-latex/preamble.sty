% Inlined guardrails from latex-preamble.instructions.md:
% - Keep existing macros stable (e.g. \argmax, \argmin, \oiiint, \oiint).
% - No package dependencies or side effects; keep lightweight and standalone.
% - Preserve math formatting (\mathop, \mathrm, \mathllap) and spacing tweaks.
% - Use UTF-8; no global layout changes.

\newcommand \Eta {\mathrm H}
\newcommand \argmax {\mathop{\mathrm{arg\,max} } }
\newcommand \argmin {\mathop{\mathrm{arg\,min} } }
\newcommand \nobreak {} % \penalty \@M
\newcommand \oiiint { {\Large {\subset\!\supset} } \mathllap \iiint }
\newcommand \oiint { {\subset\!\supset} \mathllap \iint }
